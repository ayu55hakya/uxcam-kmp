# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Kotlin
.kotlin/

# Android / local config (machine-specific — never commit)
local.properties

# IntelliJ / Android Studio
.idea/
*.iml

# macOS
.DS_Store

# Kotlin/Native & Xcode (iOS) build output
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
xcuserdata/

# CocoaPods
Pods/
*.xcworkspace
Podfile.lock

# Locally-built native iOS SDK pod (machine-specific — build from ~/Documents/ios-framework).
# Mirrors how the Android side consumes the local-debug SDK from ~/.m2.
uxcam/localpods/
