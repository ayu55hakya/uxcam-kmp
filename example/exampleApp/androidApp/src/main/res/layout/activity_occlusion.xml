<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Occlusion"
            android:textSize="22sp"
            android:paddingBottom="8dp" />

        <TextView style="@style/Section" android:text="Per-view" />
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Visible text (recorded normally)" />
        <TextView
            android:id="@+id/secretText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="8dp"
            android:text="Secret text — occluded in the recording" />
        <EditText
            android:id="@+id/passwordField"
            style="@style/Field"
            android:hint="Password (occluded)"
            android:inputType="textPassword" />
        <Button android:id="@+id/unocclude" style="@style/Action" android:text="Un-occlude password field" />
        <Button android:id="@+id/reocclude" style="@style/Action" android:text="Re-occlude password field" />
        <Button android:id="@+id/occludeWithoutGesture" style="@style/Action" android:text="Occlude password (without gesture)" />
        <Button android:id="@+id/openDialog" style="@style/Action" android:text="Open dialog (occluded field)" />

        <TextView style="@style/Section" android:text="Whole screen" />
        <Button android:id="@+id/screenOn" style="@style/Action" android:text="Occlude sensitive screen (on)" />
        <Button android:id="@+id/screenOff" style="@style/Action" android:text="Occlude sensitive screen (off)" />
        <Button android:id="@+id/screenNoGesture" style="@style/Action" android:text="Occlude screen (without gesture)" />

        <TextView style="@style/Section" android:text="All text fields" />
        <Button android:id="@+id/allTextOn" style="@style/Action" android:text="Occlude all text fields (on)" />
        <Button android:id="@+id/allTextOff" style="@style/Action" android:text="Occlude all text fields (off)" />

        <TextView android:id="@+id/status" style="@style/Status" />

    </LinearLayout>

</ScrollView>
