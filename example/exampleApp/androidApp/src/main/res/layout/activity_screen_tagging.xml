<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Screen Tagging"
            android:textSize="22sp"
            android:paddingBottom="8dp" />

        <EditText android:id="@+id/screenName" style="@style/Field" android:hint="Screen name" />
        <Button android:id="@+id/tagScreen" style="@style/Action" android:text="Tag screen name" />
        <Button android:id="@+id/autoTagOn" style="@style/Action" android:text="Automatic screen tagging (on)" />
        <Button android:id="@+id/autoTagOff" style="@style/Action" android:text="Automatic screen tagging (off)" />
        <Button android:id="@+id/improvedCaptureOn" style="@style/Action" android:text="Improved screen capture (on)" />
        <Button android:id="@+id/improvedCaptureOff" style="@style/Action" android:text="Improved screen capture (off)" />

        <TextView style="@style/Section" android:text="Ignore lists" />
        <EditText android:id="@+id/ignoreName" style="@style/Field" android:hint="Screen name to ignore" />
        <Button android:id="@+id/addIgnore" style="@style/Action" android:text="Add screen name to ignore" />
        <Button android:id="@+id/addIgnoreList" style="@style/Action" android:text="Add screen names to ignore (list)" />
        <Button android:id="@+id/removeIgnore" style="@style/Action" android:text="Remove screen name to ignore" />
        <Button android:id="@+id/removeIgnoreList" style="@style/Action" android:text="Remove screen names to ignore (list)" />
        <Button android:id="@+id/removeAllIgnore" style="@style/Action" android:text="Remove all ignored screen names" />
        <Button android:id="@+id/listIgnored" style="@style/Action" android:text="Screen names being ignored" />

        <TextView android:id="@+id/status" style="@style/Status" />

    </LinearLayout>

</ScrollView>
